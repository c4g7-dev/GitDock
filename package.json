{
  "name": "cloudvault",
  "version": "1.0.0",
  "description": "Self-hosted Git + file vault — works over port 80/443",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "express": "^5.2.1",
    "multer": "^2.1.1"
  }
}
